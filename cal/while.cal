#include "stdlib.cal"

export "C" main;

sum(i64 n) {
    i64 i;
    i64 s;

    i = 0;
    s = 0;
    while i < n {
        i = i + 1;
        s = s + i;
    }
    return s;
}

main() {
    call "C/vararg" printf("sum 10 = %ld\n", call sum(10));
    return 0;
}

